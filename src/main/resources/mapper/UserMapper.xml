<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fifth.auction.mapper.UserMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="uid">
        INSERT INTO t_user(username, password, idcard, name, mobilephone, address, email, qq, salt)
        values (#{username}, #{password}, #{idcard}, #{name}, #{mobilephone},
                #{address}, #{email}, #{qq},#{salt})
    </insert>

    <select id="findByUsername" resultType="com.fifth.auction.emtity.User">
        SELECT * FROM t_user WHERE username = #{username}
    </select>
</mapper>
