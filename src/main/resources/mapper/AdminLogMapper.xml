<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fifth.auction.mapper.AdminLogMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="uid">
        INSERT INTO t_adminlog (userid, username, usercreatetime, adminid, adminname, data)
        values (#{userid}, #{username}, #{usercreatetime}, #{adminid},#{adminname}, #{data})
    </insert>
</mapper>
